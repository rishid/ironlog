# Merged into ci.yml — this file is no longer used.
# Docker build now runs as a job in ci.yml, gated by typecheck.
name: Docker (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker workflow merged into ci.yml"
